const imprimiresultado = function(nota) {
    if (nota >= 7) {
        console.log ("mt bem vc foi aprovado")
    } else { 
        console.log ("infelizmente nn foi dessa vez")
    }
}

imprimiresultado(5)